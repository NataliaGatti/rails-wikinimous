<div class="container p-auto">
  <ul class="mt-1 ml-5 pl-5">
    <% @articles.each do |article| %>
        <li>
         <%= link_to article.title, article_path(article) %> 
        <p> <%= article.content %> </p>
        </li>
    <% end %>
  </ul>
</div>
